function incrementNum(number) {
    return number + 1;
  }
  const originalNum = 5;
  const incrementedNum = incrementNum(originalNum);
  console.log(`Original number: ${originalNum}`);
  console.log(`Incremented number: ${incrementedNum}`);